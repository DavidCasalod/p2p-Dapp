server:
nodemon --exec npm start app.js

front end:
src/app
ng serve  

sudo ./network.sh up createChannel -c mychannel -ca


sudo ./network.sh up createChannel -ca -s couchdb

sudo ./network.sh deployCC -ccn CEC-Contract-New-Version -ccp ./p2ptrading-sc/cecContract/ -ccl javascript -ccep "OR('Org1MSP.peer','Org2MSP.peer')" -cccg ./p2ptrading-sc/cecContract/collections.json